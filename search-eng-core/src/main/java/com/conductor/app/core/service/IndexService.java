package com.conductor.app.core.service;import java.util.List;import java.util.Map;import java.util.Set;import com.conductor.app.core.model.Document;public interface IndexService {	/**	 * Searches for documents that contains all of token from @param tokenString	 * @param tokenString	 * @return set of document IDs	 */	Set<Long> searchDocumentIds(String tokenString);	/**	 * update available word token to document index	 * @param document	 */		void updateIndexes(Document document);	/**	 * this should persist indexing to storage	 */	void persist();	/**	 * any in memory index structure initialization should happen here	 */	void start();}